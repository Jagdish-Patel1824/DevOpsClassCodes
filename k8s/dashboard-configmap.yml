apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  dashboard.json: "{{ lookup('file', '/home/ubuntu/monitoring/dashboard.json') }}"