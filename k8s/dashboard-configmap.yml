apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"

data:
  dashboard.json: |
    {
      "annotations": {},
      "panels": [],
      "title": "AddressBook Dashboard"
    }