plugin: amazon.aws.aws_ec2

regions:
  - us-east-1

filters:
  "tag:Roles": "app-server"

hostnames:
  - public-dns-name

keyed_groups:
  - key: tags.Name
    prefix: server

compose:
  ansible_host: public_ip_address
  ansible_user: "'ubuntu'"